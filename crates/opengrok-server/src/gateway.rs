//! What is left of the gateway, one commit from being dissolved into `agui`, `cards` and `hooks`
//! at the crate root. `HostState` itself already moved to `crate::host_state`.

pub mod cards;
pub mod conversation;
pub mod credential;
pub mod hooks;
pub mod lifecycle;
pub mod user_form;
