[package]
name = "opengrok-testdb"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "The gate-database guard for Postgres integration tests, and each test binary's own database beside the gate's."

[dependencies]
sqlx.workspace = true
tokio.workspace = true

[lints]
workspace = true
