\* The shipped start: the per-bot lock is taken before the insert's snapshot, so one start wins.
SPECIFICATION Spec
CONSTANTS Starters = {s1, s2, s3}  UseLock = TRUE  None = None
INVARIANTS AtMostOneLive
PROPERTIES EveryStartAnswers
