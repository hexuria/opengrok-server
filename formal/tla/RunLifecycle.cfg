\* This change: the claim (a retry replays, never runs), leases on resume, the stop check before an approved call, and any ended run stops its loop. Renewals may lapse.
SPECIFICATION Spec
CONSTANTS LeaseOnResume = TRUE  StopBeforeApproved = TRUE  EndedMeansStop = TRUE  RetryAttaches = TRUE
          LeaseCanLapse = TRUE  MaxSuspends = 2  MaxToolRounds = 2
INVARIANTS ApprovedAtMostOnce NoApprovedAfterStop AtMostOneStaleTool OneDriver RetryNotRefused
PROPERTIES NoOrphan
