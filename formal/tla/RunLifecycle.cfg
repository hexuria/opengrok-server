\* This change: a resumed run holds a lease and asks before running the approved call. A retry still runs a second loop (RunLifecycle_retry).
SPECIFICATION Spec
CONSTANTS LeaseOnResume = TRUE  StopBeforeApproved = TRUE  EndedMeansStop = FALSE  RetryAttaches = FALSE
          LeaseCanLapse = FALSE  MaxSuspends = 2  MaxToolRounds = 2
INVARIANTS ApprovedAtMostOnce NoApprovedAfterStop NoFalseFailure RetryNotRefused
PROPERTIES NoOrphan
