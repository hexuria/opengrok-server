\* EXPECTED TO FAIL: two starts at once both see no live lease in their snapshots and both insert, so two recipes play on one screen.
\* VIOLATES: AtMostOneLive
SPECIFICATION Spec
CONSTANTS Starters = {s1, s2}  UseLock = FALSE  None = None
INVARIANTS AtMostOneLive
