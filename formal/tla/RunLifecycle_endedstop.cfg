\* EXPECTED TO FAIL: CI's counterexample (slice2 smoke). stopped() = any ended run, without the claim, stops a same-runId retry at once.
SPECIFICATION Spec
CONSTANTS LeaseOnResume = TRUE  StopBeforeApproved = TRUE  EndedMeansStop = TRUE  RetryAttaches = FALSE
          LeaseCanLapse = FALSE  MaxSuspends = 2  MaxToolRounds = 2
INVARIANTS RetryNotRefused
PROPERTIES NoOrphan
