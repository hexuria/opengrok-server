\* EXPECTED TO FAIL: the code at 4a25af6 never retries, so a Stop landing between load and insert drops the round.
SPECIFICATION Spec
CONSTANTS Policy = "none"  Ambiguous = FALSE  MaxTries = 3
INVARIANTS RoundKept
